#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")"
PYTHON=python3
if [ -x "$(dirname "$0")/.venv/bin/python" ]; then
    PYTHON="$(dirname "$0")/.venv/bin/python"
fi
exec "$PYTHON" "$(dirname "$0")/sniff.py" "$@"
